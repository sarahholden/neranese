<footer class="site-footer">
  <div class="container">
    <!-- Social Icons -->
    {%- include 'social-icons' -%}

    <!-- Payment Icons -->
    {% if section.settings.show_payment_icons %}
      {% unless shop.enabled_payment_types == empty %}
        {%- include 'payment-icons' -%}
      {% endunless %}
    {% endif %}

    <!-- Footer Menu -->
    {%- include 'nav-menu', current_linklist: section.settings.footer_linklist -%}

    <!--   SOCIAL NAV   -->
    {%- include 'nav-menu', current_linklist: section.settings.social_linklist, open_in_new_tab: true -%}

    <!-- Copyright -->
    <div class="copyright">
      <p>{{ 'layout.footer.copyright' | t }} &copy; {{ 'now' | date: "%Y" }}, {{ shop.name | link_to: '/' }}.</p>
    </div>
  </div>
</footer>


{% schema %}
  {
    "name": "Footer",
    "settings": [
      {
        "id": "footer_linklist",
        "type": "link_list",
        "label": "First menu",
        "default": "footer",
        "info": "Update links in Online Store > Navigation."
      },
      {
        "id": "social_linklist",
        "type": "link_list",
        "label": "Social Linklist",
        "info": "Update links in Online Store > Navigation."
      },
      {
        "id": "show_payment_icons",
        "type": "checkbox",
        "label": "Show payment icons",
        "default": false
      }
    ]
  }

{% endschema %}
